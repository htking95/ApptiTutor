<!-- TO ENABLE SEARCH, run this command: rake searchkick:reindex CLASS:Userlogin -->
<p id="notice"><%= notice %></p>

<h1>Search Results AFTER the fuckup</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Email</th>
      <th>Istutor</th>
      <th>Profilepicture</th>
      <th>Isstudent</th>
      <th>Aboutme</th>
      <th colspan="3">Is there a reason for this?</th>
    </tr>
  </thead>

  <tbody>
    <% @userlogins.each do |userlogin| %>
      <tr>
        <td style="padding-right:5px"><a href="../pages/User_Profile/<%= userlogin.id %>">Profile Link</a></td>
        <td><%= userlogin.email %></td>
        <td><%= userlogin.isTutor %></td>
        <td><%= userlogin.profilePicture %></td>
        <td><%= userlogin.isStudent %></td>
        <td><%= userlogin.aboutMe %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>
