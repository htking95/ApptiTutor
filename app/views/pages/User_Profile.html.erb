<% content_for :javascript_includes do %>
  <%= javascript_include_tag 'jquery.raty.js','ratyrate.js'%>
<% end %> 

<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @userlogins.email %>
</p>

<p>
  <strong>Istutor:</strong>
  <%= @userlogins.isTutor %>
</p>

<p>
  <strong>Isstudent:</strong>
  <%= @userlogins.isStudent %>
</p>

<p>
  <strong>Aboutme:</strong>
  <%= @userlogins.aboutMe %>
</p>

<p>
	<strong>Gender:</strong>
	<%= @userlogins.gender %>
</p>

<p>
	<strong>Classes:</strong>
	<%= @userlogins.classes %>
</p>

<p>
	<string>Prices:</strong>
	<%= @userlogins.price %>
</p>

<p>
	<string>Ratings:</strong>
	<div class="row">
  		<div class="small-2 large-2 columns">
    		<%= imdb_style_rating_for @userlogins, current_userlogin%>
  		</div>
  		<br>
  		<div style="margin-left: 100px" class="small-2 large-4 columns">
    		<% if current_userlogin %>
    			RIP: <%= rating_for_user @userlogins, current_userlogin, "RIP" %>
      			Overall: <%= rating_for @userlogins, "overall", disable_after_rate: true,
      				imdb_avg: true, 
      				enable_half: true, 
      				cancel_hint: 'Cancel this vote!'%>
      			<br>
      			Clarity: <%= rating_for @userlogins, "clarity", disable_after_rate: true, 
      				imdb_avg: true,
      				enable_half: true, 
      				cancel_hint: 'Cancel this vote!' %>
      			<br>
      			Knowledge: <%= rating_for @userlogins, "knowledge",
      				disable_after_rate: true, 
      				imdb_avg: true,
      				enable_half: true, 
      				cancel_hint: 'Cancel this vote!' %>
      			<br>
      			Politeness: <%= rating_for @userlogins, "politeness", disable_after_rate: true, 
      				imdb_avg: true,
      				enable_half: true, 
      				cancel_hint: 'Cancel this vote!' %>
      			<br>
      			Flexibility: <%= rating_for @userlogins, "flexibility", disable_after_rate: true,
      				imdb_avg: true, 
      				enable_half: true,
      				cancel_hint: 'Cancel this vote!' %>
    		<% end %>
  		</div>
	</div>

</p>

<p>
	<string>Reviews:</strong>
	<%= @userlogins.reviews %>
</p>

<p>
	<string>Skills:</strong>
	<%= @userlogins.skills %>
</p>

<p>
	<string>Birthday:</strong>
	<%= @userlogins.birthday %>
</p>

